#include <iostream>
using namespace std;

int main()
{
    int arr[]={100,90,80,78,90};
    int *ptr=arr;
    int size=sizeof(arr)/sizeof(arr[0]);
    int sum=0;
    
    for(int i=0;i<size;i++){
        sum=sum+*(ptr+i);
    }
    cout<<sum;

    return 0;
}
