#include <iostream>
using namespace std;

int main()
{
    int arr[]={10,20,90,100,60};
    int *ptr=arr; //pointer to first element arr[0]
    int n=sizeof(arr)/sizeof(arr[0]);
    int sum;
    
    for(int i=0;i<n;i++){
        sum=sum+*(ptr+i);
    }
    cout<<sum;
    
   
   

    return 0;
}
