#include <iostream>
#include <string>

using namespace std;

int main() {
    int customerId;
    string customerName;
    float unitsConsumed, totalAmount;

    cout << "Enter customer ID: ";
    cin >> customerId;

    cout << "Enter customer name: ";
    cin.ignore();  
    getline(cin, customerName);

    cout << "Enter units consumed: ";
    cin >> unitsConsumed;

    
    if (unitsConsumed <= 100) {
        totalAmount = unitsConsumed * 2.5;
    } else if (unitsConsumed <= 200) {
        totalAmount = 100 * 2.5 + (unitsConsumed - 100) * 4.0;
    } else if (unitsConsumed <= 300) {
        totalAmount = 100 *
